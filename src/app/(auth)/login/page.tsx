import { LoginForm } from "@/components/auth/login-form";

export default function LoginPage() {
  return (
    <div className="login-bg flex min-h-screen items-center justify-center p-4 sm:p-6">
      <LoginForm />
    </div>
  );
}
